server:
  port: 8080
spring:
  datasource:
    url: jdbc:mysql://localhost:3306/blog?serverTimezone=UTC&useSSL=false&allowPublicKeyRetrieval = true
    username: root
    password: 123456789
  jpa:
    show-sql: true
    hibernate:
      ddl-auto: update